{
  "name": "esfenix-landing-page",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "build:catalog-data": "node scripts/build-catalog-data.mjs",
    "check:fresa-map": "node scripts/check-fresa-map.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "vite": "^8.1.1"
  }
}
